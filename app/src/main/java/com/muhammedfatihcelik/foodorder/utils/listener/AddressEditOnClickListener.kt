package com.muhammedfatihcelik.foodorder.utils.listener

import com.muhammedfatihcelik.foodorder.data.entity.address.Address

interface AddressEditOnClickListener {
    fun onClick(address: Address)
}